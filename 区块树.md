1.任务id

2.加锁



实现一个树状结构，放到一个独立的文件，其他地方暂时不用它，这个结构是一个个的节点连接起来。像树一样会生长。这个树有最大高度，也就是说从根到最长的叶子节点有最大高度，当新插入节点时，发现它的最大高度超出了限制，就要删除根节点之前的数据，以及基于这些节点产生的数据。函数有插入，剪枝。

最近不可逆节点：从当前节点根据parentKey往前找不可逆数量个节点，这个节点就是最近不可逆节点。或者是parent节点的最近不可逆节点的parent节点。



成员变量

（1）树结构：

keyValue  key->nodeValue

parentToChild  parentKey->key->{}

headers，key->{}

root， value指针



（2）孤块集：

orphan parentKey->key->value

孤块value是height、key、parentKey、weight



（3）不可逆数量



key是string

value是height，key、parentKey，weight

nodeValue是value加上不可逆高度，不可逆key

可以单独声明key和value的类型，方便替换这两种类型。



插入函数，传参是height，key、parentKey，weight

从keyValue里检查它是否存在，如果存在直接返回。

从keyValue里找它的parent，

1.如果有parent或root为空

如果root为空，设置root。

调用内部插入函数。

剪枝。



2.否则

添加到孤块。



插入函数返回插入的记录列表。每一项记录都需要nodeValue。



内部插入函数：

（1）设置到keyValue，设置到parentToKey，从headers里移除它的parent（不检查是否存在），把它自己设置到headers。

（2）调用检查孤块函数。



剪枝函数：

遍历每个headers，找出一个最高高度的value。高度如果相同，找权重更大的。

如果root比最长链的最近不可逆高度低，就要剪枝。删除从root到新root之前的所有数据。

从最新不可逆节点往前循环遍历，对于每个parent，调用一个递归的walk函数，这个walk函数里从parentToChild里找到所有的node，当找不到时，从headers里删除。



检查孤块函数：

找到孤块，遍历同一层级的，每一个节点调用内部插入函数。





1.如何获取待同步的任务

2.创建任务是根据hash获取还是高度获取

3.



同步时，同步每个孤块的parent，如果没有孤块，就同步更高高度。

如果远程高度相差不大，可以插入远程块作为孤块？？





1.当最高高度和节点最高高度相差超过2个不可逆区间时，向大于这个不可逆数量的节点进行同步。

2.





1.给出的是高度

（1）根据高度直接同步



1.先同步孤块的parent，这是根据hash去同步

2.获得一系列列表

3.从同步任务里检查





1.根据高度同步数据

2.



2.同步到之后，写入区块

3.



1.树表示了什么？

区块头？先确保区块头是可以衔接的？怎么先获取区块头，

怎么检查这些区块头的数据是没有同步的？





（1）树结构只包含区块头

（2）另外一个set是记录了待同步列表？







1.怎么构建树，可以从本地获取最新数据，待定。

2.怎么获取任务。

3.怎么推进任务？



1.获取区块头，



方案1

树中的数据表示这个数据是已经同步完毕的。

1.直接按照高度进行同步，数据直接放入

2.获取其中空档部分，用hash进行同步。



一开始用高度并行同步，然后用hash并行同步？



方案2

树中的数据只是表示有区块头，还需要额外的数据结构去表示待同步的数据。



1.用额外的一个数据结构描述了其中的数据信息。当块往前推进时，要删除分支上的数据。

2.需要快速同步区块头。可以用高度同步。用hash并行同步？

3.遍历每条分支，检查需要同步哪些数据。



需要同步区块头和实际的区块数据





1.区块树

2.区块树中已所有的数据，如果有数据就是在里面。





1.不断扩展区块边界，但是不能超出2个不可逆区间

2.获取header树上的数据



（1）并行获取高度之间的区块头

直接并行获取，不用考虑节点，然后插入到区块树中



（2）获取缺失的区块













修改剪枝函数，参数增加数量，也就是从root开始往后删除多少个高度，同时要把分支上的节点都要删除。并且最长链和root之间的高度差不能小于不可逆数量，返回被剪枝掉的节点。







fetch_manager里增加一个成员变量storagedBlock，类型是hash->{}

fetch_manager增加一个成员变taskManager，类型是hash->{}





1.获取区块树的起始和结束高度。如果没有数据，就需要先获取第一个高度的区块头。返回。

2.区块树的起始到结束高度如果没有达到2倍的不可逆区块数的大小，就同步区块头直到达到这个大小。

3.获取区块树中的当前孤块往前的节点，同步这些节点。最好有高度。根据区块hash或高度去同步区块头。同步任务写入taskManager。

4.获得区块树中从低到高的分支（最好有权重）。遍历每一个分支，如果当前节点有数据但未写入且前一个块是已写入的（或者不存在，没有不可逆块表示没有parent），就写入，然后修改状态。如果是没数据的，去同步。

5.检查是否能够剪枝，剪枝时，返回被剪枝的区块，然后从写入列表里删除。



剪枝的条件：

（1）已经存储区块的起始和结束高度超过了不可逆数量



剪多少个块？

（1）保留数量要大于不可逆区间大小，剩余的数量要剪枝





1.应该有个成为leader时的通知



fetchmanager里增加函数，用户成为leader和失去leader时执行

（1）成为leader，从数据库里加载最新的一批区块到blocktree中，如果加载失败，释放leader。如果没有数据，从远端获取数据。如果获取失败，释放leader。

（2）失去leader，释放blocktree、清空taskmanager、storagedmanager、所有节点都设置为空闲。



成为leader，读取从最后一个块往前到不可逆区间内+1高度内的所有区块，如果区块状态是complete的，就设置到storagedBlock



1.如果为空，从数据库里加载最新的一批区块

2.如果数据库为空，读取指定的高度。

3.后续不会更新。



如果leader更新了怎么办？

（1）清空存储



DoWithLeaderElection增加两个传参，放在RunAsLeader前后

（1）成为leader执行的函数

（3）失去leader执行的函数







1.同步区块头通知？

2.根据孤块获取parent时通知

3.同步到body要通知？

4.需要一个通知回调数组。收到后，会先检查。回调通知里有带函数指针。





1.如果已经存在，检查状态，如果是已经完成的，返回失败。如果未完毕的，获得记录id。

2.如果不存在，写入。获得记录id。





1.写入区块发生冲突时忽略。

2.读取区块，如果状态是完毕的，返回失败。如果未完毕，获得记录id。





1.更新行，且行的状态是未完毕的，看影响的行。



构造出分叉







3.fetch目录下实现DbOperator，包含了，实现写入数据和读取数据的接口。但是这个的目的只是为了给fetchmanager用

5.实现一个数据同步接口，根据传递的参数获取数据。



1.实现block_tree的测试，输出测试用例

2



整个模型是同步区块头、然后同步区块内的内容、

（1）扩展区块树

（1）同步区块头（可以创建协程直接同步）标记这些区块的状态、区块头同步状态（按照高度和hash进行区分）

（2）同步未同步区块体



1.fetch里的存储用一个interface包装

2.





1.创建vault

2.获取钱包地址

3.白名单？

4.查看资金？什么时候查看，登陆的时候查看。刷新的时候会主动获取一次，（）

5.铸造。金库给他发送资金。订单是异步的。

5.转账也是异步的。主动发起请求，可能需要审批。

6.销毁，本质上也是转账，可能也是需要审批的。

7.



1.发起请求

2.异步确认。确认转账，确认销毁，确认铸造（管理端）。





1.用户的操作很麻烦，需要给他管理vault、而且用户需要管理自己的gas。











